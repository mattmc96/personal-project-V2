{
  "name": "personal-project_v2",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "backend": "cd backend && yarn start:dev",
    "start": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
